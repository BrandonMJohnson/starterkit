{
  "name": "starterkit-ui",
  "root": "services/node/starterkit-ui",
  "sourceRoot": "services/node/starterkit-ui/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @starterkit/starterkit-ui build",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @starterkit/starterkit-ui test",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @starterkit/starterkit-ui lint",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
