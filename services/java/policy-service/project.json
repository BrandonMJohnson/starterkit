{
  "name": "policy-service",
  "root": "services/java/policy-service",
  "sourceRoot": "services/java/policy-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :services:java:policy-service:build",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :services:java:policy-service:test",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
