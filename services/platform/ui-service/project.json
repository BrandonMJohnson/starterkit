{
  "name": "ui-service",
  "root": "services/platform/ui-service",
  "sourceRoot": "services/platform/ui-service",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :services:platform:ui-service:build",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :services:platform:ui-service:test",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :services:platform:ui-service:lint",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
