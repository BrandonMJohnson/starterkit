{
  "name": "persistence",
  "root": "libs/java/persistence",
  "sourceRoot": "libs/java/persistence/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :libs:java:persistence:build",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :libs:java:persistence:test",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
