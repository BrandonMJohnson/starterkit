{
  "name": "ui-service",
  "root": "apps/platform/ui-service",
  "sourceRoot": "apps/platform/ui-service",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :apps:platform:ui-service:build",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :apps:platform:ui-service:test",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./java-build/gradlew -p java-build :apps:platform:ui-service:lint",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
